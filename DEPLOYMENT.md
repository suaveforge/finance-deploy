# FINANCE static frontend
Captured from the existing live FINANCE console and staged for SiteHub integration.
The deployment replaces only the default API origin with the verified `api-finance` proxy endpoint; user-selected `trade_server` values in localStorage remain supported.
